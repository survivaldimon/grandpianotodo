# SESSION 2026-01-06: Онбординг преподавателя

## Обзор

Реализован полный flow онбординга для новых преподавателей при присоединении к заведению по коду приглашения.

---

## Что сделано

### 1. Экран онбординга преподавателя

**Файл:** `lib/features/institution/screens/teacher_onboarding_screen.dart`

**Функциональность:**
- PageView с двумя шагами:
  1. **Выбор цвета** — для отображения занятий в расписании
  2. **Выбор направлений** — чекбоксы с предметами заведения (множественный выбор)
- Можно пропустить любой шаг кнопкой "Пропустить"
- Сохраняются только изменения (не дублируются существующие данные)
- После завершения — редирект на Dashboard

**UI:**
```
┌─────────────────────────────────┐
│  ● ○           [Пропустить]     │
│                                 │
│         🎨 (иконка)             │
│                                 │
│     Добро пожаловать!           │
│                                 │
│   Выберите свой цвет            │
│  ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○       │
│       [🎨 Палитра]              │
│                                 │
│        [   Далее →   ]          │
└─────────────────────────────────┘
```

### 2. Баннер на Dashboard

**Файл:** `lib/features/dashboard/screens/dashboard_screen.dart`

Если преподаватель не заполнил цвет или направления — показывается градиентный баннер сверху:
- Текст: "Завершите настройку профиля"
- Кнопка "Заполнить" → открывает онбординг

**Провайдер проверки:**
```dart
// lib/features/institution/providers/member_provider.dart
final needsOnboardingProvider = Provider.family<bool, String>((ref, institutionId) {
  // Возвращает true если:
  // - Пользователь НЕ владелец
  // - Нет цвета ИЛИ нет направлений
  // Возвращает false пока данные загружаются (избегаем мелькания)
});
```

### 3. Редактирование направлений в настройках

**Файл:** `lib/features/institution/screens/members_screen.dart`

Добавлен пункт меню "Направления" для каждого участника:
- Себе — всегда доступно
- Другим — только для владельца/администратора

**UI диалога:**
```
┌─────────────────────────────────┐
│  📚 Направления      [Сохранить]│
│     Имя участника               │
├─────────────────────────────────┤
│  🎵 [✓] Фортепиано              │
│  🎵 [ ] Вокал                   │
│  🎵 [✓] Гитара                  │
│  🎵 [ ] Скрипка                 │
└─────────────────────────────────┘
```

### 4. Исправление сохранения цвета

**Проблема:** Цвет не сохранялся в базу данных.

**Причины:**
1. Цвет передавался с `#` (например `#3F51B5`), но в базе хранится без `#` (`3F51B5`)
2. RLS политика не разрешала участникам обновлять свою запись

**Решения:**

**a) Очистка формата цвета:**
```dart
// lib/features/institution/providers/member_provider.dart
Future<bool> updateColor(String memberId, String institutionId, String? color) async {
  final cleanColor = color?.replaceAll('#', '').toUpperCase();
  await _repo.updateMemberColor(memberId, cleanColor);
  // ...
}
```

**b) Проверка результата UPDATE:**
```dart
// lib/features/institution/repositories/institution_repository.dart
Future<void> updateMemberColor(String memberId, String? color) async {
  final result = await _client
      .from('institution_members')
      .update({'color': color})
      .eq('id', memberId)
      .select();  // ← Добавлено для проверки результата

  if ((result as List).isEmpty) {
    throw DatabaseException('Не удалось обновить цвет (RLS или запись не найдена)');
  }
}
```

**c) RLS политика (выполнить в Supabase):**
```sql
CREATE POLICY "Members can update own color"
ON institution_members
FOR UPDATE
USING (user_id = auth.uid())
WITH CHECK (user_id = auth.uid());
```

### 5. Realtime обновления для участников

**SQL для включения realtime:**
```sql
ALTER PUBLICATION supabase_realtime ADD TABLE institution_members;
```

После этого изменения цвета/направлений будут видны другим участникам без перезапуска.

---

## Маршрутизация

**Файл:** `lib/core/router/app_router.dart`

Добавлен маршрут:
```dart
// Вне ShellRoute — полноэкранный flow
GoRoute(
  path: '/institutions/:institutionId/onboarding',
  builder: (context, state) => TeacherOnboardingScreen(
    institutionId: state.pathParameters['institutionId']!,
  ),
),
```

**Редирект после присоединения:**
```dart
// lib/features/institution/screens/join_institution_screen.dart
// После успешного joinByCode():
context.go('/institutions/${institution.id}/onboarding');
```

---

## Структура данных

### Таблицы

| Таблица | Поле | Описание |
|---------|------|----------|
| `institution_members` | `color` | TEXT, цвет без `#` (например `3F51B5`) |
| `teacher_subjects` | - | Связь user_id → subject_id |

### Провайдеры

| Провайдер | Тип | Описание |
|-----------|-----|----------|
| `needsOnboardingProvider` | Provider.family | Проверка необходимости онбординга |
| `myMembershipProvider` | StreamProvider.family | Данные текущего пользователя как участника |
| `teacherSubjectsProvider` | FutureProvider.family | Направления преподавателя |
| `teacherSubjectsControllerProvider` | StateNotifierProvider | CRUD для направлений |

---

## Файлы изменены/созданы

| Файл | Изменения |
|------|-----------|
| `lib/features/institution/screens/teacher_onboarding_screen.dart` | **Новый** — экран онбординга |
| `lib/features/institution/screens/members_screen.dart` | Добавлен диалог "Направления" |
| `lib/features/institution/screens/join_institution_screen.dart` | Редирект на онбординг |
| `lib/features/institution/providers/member_provider.dart` | `needsOnboardingProvider`, исправление `updateColor` |
| `lib/features/institution/repositories/institution_repository.dart` | Проверка результата UPDATE |
| `lib/features/dashboard/screens/dashboard_screen.dart` | Баннер онбординга |
| `lib/core/router/app_router.dart` | Маршрут `/onboarding` |

---

## SQL миграции

```sql
-- 1. RLS политика для обновления цвета
CREATE POLICY "Members can update own color"
ON institution_members
FOR UPDATE
USING (user_id = auth.uid())
WITH CHECK (user_id = auth.uid());

-- 2. Realtime для участников
ALTER PUBLICATION supabase_realtime ADD TABLE institution_members;
```

---

## Тестирование

### Сценарий 1: Новый преподаватель
1. Присоединиться к заведению по коду
2. Появится экран онбординга
3. Выбрать цвет → Далее
4. Выбрать направления → Готово
5. Переход на Dashboard без баннера

### Сценарий 2: Пропуск онбординга
1. Присоединиться к заведению
2. Пропустить оба шага
3. На Dashboard — баннер "Завершите настройку"
4. Клик на баннер → онбординг

### Сценарий 3: Редактирование направлений
1. Участники → найти себя → меню (⋮)
2. "Направления"
3. Изменить чекбоксы → Сохранить

---

## Известные особенности

1. **Баннер не мелькает** — `needsOnboardingProvider` возвращает `false` пока данные загружаются
2. **Только новые данные сохраняются** — при повторном онбординге не дублируются существующие направления
3. **Цвет нормализуется** — `#3F51B5` → `3F51B5` перед сохранением
4. **Владельцу онбординг не нужен** — баннер не показывается
